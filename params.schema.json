{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "cacheHours": {
      "title": "Cache time in hours",
      "type": "number"
    },
    "params": {
      "title": "Define types for ",
      "type": "array",
      "items": {
        "$ref": "#/definitions/Params"
      }
    }
  },
  "additionalProperties": false,
  "required": [
    "cacheHours"
  ],
  "definitions": {
    "Params": {
      "type": "object",
      "properties": {
        "name": {
          "title": "URL Search param name for the query",
          "type": "string"
        },
        "replaces": {
          "title": "Replacing substring in the query sql template",
          "description": "Replacing uses `String.replaceAll`, please make sure the replacing substring is not substring of other texts",
          "type": "string"
        },
        "template": {
          "type": "object",
          "title": "Replacing templates, mapped by search value",
          "additionalProperties": {
            "type": "string"
          }
        },
        "enums": {
          "title": "Enums for prefetching and validating",
          "oneOf": [
            {
              "title": "The allowed values and should be prefetched",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "title": "Prevent any prefetching and allow any input.",
              "type": "boolean",
              "enum": [
                false
              ]
            }
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "replaces",
        "enums"
      ]
    }
  }
}